const userInput = 10;
let result;

result = 18 + userInput;

result = result - 4;
result = result * 2;
result = result / 6;

alert ('userInput = ' + userInput);
alert ('result = ' + result);
/* 
The value of the user input did not change. It is unaffected and we did update the value of that variable.  
 Only the value of the result would have changed and because that is what we are manipulating 
 */

